<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookmark">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookMark">
        <activity android:name=".product_review.ReviewActivity" />
        <activity android:name=".user_management.UserActivity" />
        <activity android:name=".payment_management.PaymentActivity" />
        <activity android:name=".order_management.OrderActivity" />
        <activity android:name=".listing_management.ListingActivity" />
        <activity android:name=".category_management.CategoryActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>